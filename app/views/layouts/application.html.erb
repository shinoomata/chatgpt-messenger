<!DOCTYPE html>
<html>
  <head>
    <title>ChatgptMessenger</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <div class="navbar">
      <div class="centered-text">
          <% if user_signed_in? %>
          <%= button_to "ログアウト", logout_path, method: :delete, 
          data: { turbo_confirm: "ログアウトしますか?" }, class: "btn btn-logout" %>
          <% else %>
          <%= button_to "Xでログイン", "/auth/twitter2", method: :post,
          data: { turbo: false }, class: "btn btn-login" %>
          <% end %>
      </div>
    </div>

  </body>

  
</html>
